\documentclass[../analysis1.tex]{subfiles}

\begin{document}


\chapter{Taylor's Theorem}

This chapter is motivated by the question: how can we approximate a function with other, simpler functions, locally? In particular, the approximation of functions by \textit{polynomials}. Polynomials are the natural first choice as they are, in some way, the simplest type of function on $\R$ and have very nice properties. 

When discussing Taylor's theorem, we are fundamentally talking about \textit{polynomials}, not power series. A series that defines a function is not just a single series; for every $x$, you have a different limit. Establishing whether such a series converges, is continuous, or is differentiable requires significant machinery in general, but for functions with a finite Taylor polynomial, these properties are automatic because of the algebraic properties of polynomials.

\section{Taylor Polynomials}

\begin{definition}[Taylor Polynomial]
    \begin{shaded*}
        Let $f$ be $n$ times differentiable at a point $a$. The $n$-th order \textit{Taylor polynomial} of $f$ centred at $a$ is:
        \[
            P_{n,a}(x) \coloneqq \sum_{k=0}^n \frac{f^{(k)}(a)}{k!} (x-a)^k.
        \]
    \end{shaded*}
\end{definition}

\begin{remark}
    We can construct a polynomial with prescribed derivatives at a point $a$ by writing it in the ``centred'' form $\sum_{k=0}^n c_k(x-a)^k$. The derivatives of this polynomial at $a$ directly determine the coefficients $c_k = f^{(k)}(a)/k!$.
    
    Geometrically, by forcing the polynomial and the function to share the exact same value, slope, concavity, and higher-order rates of change at a single point $a$, we are essentially locking their \textit{local geometries} together. The higher the order $n$, the more rigid this lock becomes, widening the neighbourhood where the polynomial stays glued to the function before inevitably peeling away.

    It may seem slightly unnatural to construct a polynomial whose derivatives up to order $n$ to act as a local approximation to a given function, but to see why this works it is useful to consider the \textit{remainder} function for a given function and its Taylor polynomial:
\end{remark}

\begin{definition}[The Remainder]
    \begin{shaded*}
        The \textit{remainder} of the Taylor approximation of order $n$ is the difference between the function and its Taylor polynomial:
        \[
            R_{n,a}(x) \coloneqq f(x) - P_{n,a}(x).
        \]
    \end{shaded*}
\end{definition}

The remainder $R_{n,a}(x)$ represents the exact ``error'' in our approximation. By construction, the function and the polynomial share their first $n$ derivatives at $a$. Therefore, the remainder function has its first $n$ derivatives vanishing at $a$.
    
Geometrically, this corresponds to extreme ``flatness'' at $a$ (similar to how $y=x^{n}$ is extremely flat near the origin, for $n$ large - see below). Because the error is practically zero in this flat region, the polynomial ``hugs'' the function tightly. 

\begin{center}
    \begin{tikzpicture}[scale=0.7]
        \useasboundingbox (-4.5, -1) rectangle (4.5, 6);
        % Axes
        \draw[->] (-4,0) -- (4,0) node[right] {$x$};
        \draw[->] (0,-0.5) -- (0,5) node[above] {$y$};
        \draw[thick, black, domain=-4:4, samples = 200] plot (\x, {(\x/3.5)^(10)}) node[above right]{$y=x^{10}$};
    \end{tikzpicture}
\end{center}

\begin{definition}[Little-$o$ Notation]
    \begin{shaded*}
        Let $k \in \N$. We say that $f \in o(|x-a|^k)$ if
        \[
            \lim_{x \to a} \frac{f(x)}{|x-a|^k} = 0.
        \]
    \end{shaded*}
\end{definition}

Conceptually, the extreme flatness of the remainder is demonstrated in the above Little-$o$ notation. As we will show below, $R_{n,a} \in o(|x-a|^n)$. 
    
This limit explicitly states that as $x$ approaches $a$, the error $R_{n,a}(x)$ shrinks to zero strictly \textit{faster} than the $n$-th power of the distance from the centre. The error is strictly dominated by the polynomial $|x-a|^n$. This is the formal mathematical translation of the polynomial ``hugging'' the function: the approximation error vanishes so rapidly that it is negligible even when compared to the microscopic scale of $|x-a|^n$.


\begin{center}

\begin{tikzpicture}[scale=1]
    
    \useasboundingbox (-4.5, -2) rectangle (4.5, 3);

    % Draw basic, clean axes
    \draw[->] (-3.2, 0) -- (3.2, 0) node[right, black] {$x$};
    \draw[->] (0, -2.2) -- (0, 2.2) node[above, black] {$y$};

    % The original function: f(x) = sin(x)
    % Note: 'r' converts radians to degrees for TikZ's native trig functions
    \draw[domain=-3.1:3.1, samples=100, black, thick] 
        plot (\x, {sin(\x r)});
        
    % The Taylor approximation: P_3(x) = x - x^3 / 6
    \draw[domain=-2.8:2.8, samples=100, red, thick] 
        plot (\x, {\x - (\x^3)/6}) node[below right]{$P_{3,0}(x)$};

    % Mark the point of approximation 'a'
    \filldraw (0,0) circle (1.5pt) node[below right] {$a=0$};
    \node at (3,0.9) {$\sin(x)$};

\end{tikzpicture}
\end{center}

\begin{theorem}[Taylor's Theorem with Lagrange Remainder]
\label{thm:ttl}
    \begin{shaded*}
        Let $f$ be $n+1$ times differentiable on an open interval containing $a$ and $x$. Then there exists some $\theta$ strictly between $a$ and $x$ such that:
        \[
            R_{n,a}(x) = \frac{f^{(n+1)}(\theta)}{(n+1)!} (x-a)^{n+1}.
        \]
    \end{shaded*}
\end{theorem}

\begin{proof}
    Fix $x \neq a$. Consider the function $F(t)$ defined on the closed interval between $a$ and $x$:
    \[
        F(t) \coloneqq f(x) - \sum_{k=0}^n \frac{f^{(k)}(t)}{k!} (x-t)^k.
    \]
    Notice that $F(x) = 0$ and $F(a) = R_{n,a}(x)$. Differentiating $F(t)$ with respect to $t$ causes the sum to telescope, leaving only the final term:
    \[
        F'(t) = - \frac{f^{(n+1)}(t)}{n!} (x-t)^n.
    \]
    Now, define an auxiliary function $G(t)$ to apply Rolle's Theorem:
    \[
        G(t) \coloneqq F(t) - \left( \frac{x-t}{x-a} \right)^{n+1} F(a).
    \]
    We have $G(x) = 0$ and $G(a) = F(a) - F(a) = 0$. Since $G$ is continuous on the closed interval between $a$ and $x$ and differentiable on the open interval, Rolle's Theorem guarantees a point $\theta$ strictly between $a$ and $x$ where $G'(\theta) = 0$. 
    
    Differentiating $G(t)$ gives:
    \[
        G'(t) = F'(t) + (n+1)\frac{(x-t)^n}{(x-a)^{n+1}} F(a).
    \]
    Setting $G'(\theta) = 0$ and substituting $F'(\theta)$ yields:
    \[
        \frac{f^{(n+1)}(\theta)}{n!} (x-\theta)^n = (n+1)\frac{(x-\theta)^n}{(x-a)^{n+1}} R_{n,a}(x).
    \]
    Cancelling $(x-\theta)^n$ (since $\theta \neq x$) and rearranging gives the desired form for $R_{n,a}(x)$.
\end{proof}

\begin{remark}
    The Lagrange form is incredibly convenient for rough approximations. If the $(n+1)$-th derivative is bounded, the remainder is immediately bounded. However, it has significant drawbacks: the value $\theta$ depends on $x$ in a way that is not explicitly understood and is generally not continuous. Therefore, if we want to differentiate the remainder or evaluate it precisely, the Lagrange form is inadequate.
\end{remark}

\begin{theorem}[Taylor's Theorem with Integral Remainder]
\label{thm:tti}
    \begin{shaded*}
        Let $f$ be $n+1$ times continuously differentiable on an open interval containing $a$. For any $x$ in this interval,
        \[
            R_{n,a}(x) = \int_a^x \frac{f^{(n+1)}(t)}{n!} (x-t)^n \d{t}.
        \]
    \end{shaded*}
\end{theorem}

\begin{proof}
    We proceed by mathematical induction on $n$. 
    For $n=0$, the theorem states $f(x) - f(a) = \int_a^x f'(t) \d{t}$, which is exactly the Fundamental Theorem of Calculus.
    
    Assume the formula holds for some $k$. We apply integration by parts to the integral remainder for $k$. Let $u = f^{(k+1)}(t)$ and $v = -\frac{(x-t)^{k+1}}{k+1}$. Then $\d{u} = f^{(k+2)}(t)\d{t}$ and $\d{v} = (x-t)^k\d{t}$.
    \[
        \int_a^x \frac{f^{(k+1)}(t)}{k!} (x-t)^k \d{t} = \left[ -f^{(k+1)}(t) \frac{(x-t)^{k+1}}{(k+1)!} \right]_a^x + \int_a^x \frac{f^{(k+2)}(t)}{(k+1)!} (x-t)^{k+1} \d{t}.
    \]
    Evaluating the boundary term at $x$ yields $0$. Evaluating at $a$ yields $\frac{f^{(k+1)}(a)}{(k+1)!}(x-a)^{k+1}$. Thus,
    \[
        R_{k,a}(x) = \frac{f^{(k+1)}(a)}{(k+1)!}(x-a)^{k+1} + \int_a^x \frac{f^{(k+2)}(t)}{(k+1)!} (x-t)^{k+1} \d{t}.
    \]
    Since $R_{k,a}(x) - \frac{f^{(k+1)}(a)}{(k+1)!}(x-a)^{k+1} = R_{k+1,a}(x)$, the result holds for $n = k+1$.
\end{proof}

\begin{remark}
    The integral remainder provides an \textit{explicit} formula for the remainder that works for every $x$. Because the dependence on $x$ is explicitly captured inside the integral (and not hidden in an arbitrary $\theta$), we can use the Fundamental Theorem of Calculus to differentiate the remainder, bound it precisely, and evaluate it anywhere.
\end{remark}

\section{Taylor Series}


We have seen that Taylor polynomials are fundamentally local approximations, however there is a special class of functions, called \textit{analytic functions}, whose Taylor polynomials converge to the original function on an interval as $n\to\infty$. This is quite surprising, as it says that these functions can be recovered \textit{globally} in the form of an ``infinite polynomial'' from only their derivatives \textit{at a single point}, i.e. from only local information. It may be counter-intuitive that purely local information - derivatives evaluated at a single microscopic point $a$ - can perfectly reconstruct a function's global shape. If you draw an arbitrary curve by hand, knowing its slope at $x=0$ tells you absolutely nothing about its height at $x=10$.

However, polynomials are extremely rigid. A polynomial of degree $n$ cannot suddenly change its asymptotic behaviour; its behaviour on the real line is entirely determined by its finite coefficients. In other words, its global behaviour is completely determined by a finite set of points. Because an analytic function is essentially an ``infinite polynomial'', it inherits this ``rigidity''. If you know a real analytic function and all of its derivatives at a single point, you possess all the information you need to rebuild the function. There is zero room for the function to do anything unexpected globally because its global behaviour is a rigid algebraic consequence of its local derivatives.

Indeed, these \textit{are} the special functions defined by this property; there are many examples, as we will see below, of functions that are in $C^{\infty}$ (infinitely differentiable) but are not analytic.

\begin{definition}[Real Analytic Function]
    \begin{shaded*}
        We say that a function $f\in C^{\infty}$ is \textit{real analytic} at a point $a$ if there exists $R>0$ such that for every $x\in (a-R,a+R)$ we have $\lim_{n\to\infty}P_{n,a}(x)=f(x)$. In this case, we write
        \[f(x)=\sum_{n=0}^{\infty}\frac{f^{(n)}(a)}{n!}(x-a)^n.\]
        The maximal $R>0$ for which this property holds is called the \textit{radius of convergence} of the Taylor series.
    \end{shaded*}
\end{definition}

Intuitively, this means that the function is ``rigid'' enough that it can be approached globally (i.e. on an interval) by polynomial functions. This is precisely why the derivatives of the function at one point generate can build the whole function; as we saw in the discussion at the start of this chapter, polynomials are extremely special functions that can be re-centred to any point while remaining invariant, and so the local information of an analytic function inherits this property, such that local information, like derivatives at a point $a$, can still recover the function globally. Where we ``centre'' our approximation does not matter, as long as it is inside the interval of convergence; in the limit, the approximation recovers the whole function.

\begin{example}
    Not all functions in $C^{\infty}$ are (real) analytic. The idea is to construct a $C^{\infty}$ function $f:\R\to\R$ whose graph, though not horizontal, is ``very flat'' near $0$ in the sense that all of its derivatives vanish at $0$. A classic example is the bump function $f:\R\to\R$ defined by
    \[
        f(x) = \begin{cases} 
        \exp(-1/x^2), & x \neq 0, \\ 
        0, & x = 0. 
        \end{cases}
    \]
    \begin{center}
        % \definecolor{mred}{RGB}{220, 50, 50} % Ensure mred is defined in your document preamble
        \begin{tikzpicture}[scale=1]
          % Axes
          \draw [->] (-3, 0) -- (3, 0) node [right] {$x$};
          \draw [->] (0, -0.5) -- (0, 2) node [above] {$y$};
      
          \begin{scope}
            % Clip keeps everything inside the visual boundaries
            \clip (-3, -0.5) rectangle (3, 1.9);
      
            % \d is the tightly pre-calculated domain boundary for each polynomial.
            % This completely prevents TikZ from attempting to calculate massive numbers
            % like 720 * (2^12), which causes the "Dimension too large" error.
            \foreach \n/\fac/\d in {
              1/1/1.6, 
              2/2/1.1, 
              3/6/0.9, 
              4/24/0.75, 
              5/120/0.68, 
              6/720/0.63
            } {
              \pgfmathsetmacro\c{40 + 10 * \n};
              
              % The upper bound polynomials: n! * x^(2n)
              \draw [domain=-\d:\d, samples=150, mred!\c!white, thick] 
                plot (\x, {\fac * (\x*\x)^\n});
            }
      
            % The bump function: e^(-1/x^2)
            % Bridged smoothly across 0 to avoid division by zero errors
            \draw [thick, black] 
              plot [domain=-3:-0.05, samples=100] (\x, {exp(-1/(\x*\x))}) 
              -- (0,0) 
              -- plot [domain=0.05:3, samples=100] (\x, {exp(-1/(\x*\x))})
            node[above left]{$f(x)$};
      
          \end{scope}
        \end{tikzpicture}
      \end{center}
    
    This function is infinitely differentiable everywhere (which one should verify), but $f^{(n)}(0) = 0$ for all $n \in \N$. Therefore, its Taylor polynomial at $a=0$ is the identically zero function, $P_{n,0}(x) = 0$ for all $n$.

    Intuitively, becauase this function converges to zero as $x\to 0$ faster than any polynomial, each power $x^n$ is ``too weak'' to match the exponential decay of $f$ near 0, so has nothing to contribute to the Taylor polynomial at 0. Precisely, it is because $f\in o(|x|^n)$ for every $n\in\N$; the function itself vanishes faster than any polynomial term $x^n$ can measure, so its best polynomial approximation at the origin is simply the zero polynomial, $P_{n,0}(x) = 0$. The polynomials literally cannot ``see'' the function's global growth because it is locally too flat.
\end{example}

Based on the Lagrange form of the remainder in Taylor's Theorem, we get a useful sufficient condition for the convergence of Taylor series to the original function.

\begin{theorem}
    \begin{shaded*}
        Let $f\in C^{\infty}$ be defined on $(a-\rho, a+\rho)$ for some $\rho>0$. If there exists $C,R > 0$ and $N\in\N$ such that for all $x \in (a-\rho, a+\rho)$ and all $n \ge N$,
        \[
            |f^{(n+1)}(x)| \le C \frac{(n+1)!}{R^{n+1}},
        \]
        then the Taylor polynomial $P_{n,a}(x)$ of $f$ converges to $f(x)$ as $n\to\infty$ for every $x$ satisfying \[|x-a|<\min\{\rho,R\}.\]
        In particular, $f$ is analytic on $|x-a|<\min\{\rho,R\}$.
    \end{shaded*}
\end{theorem}

\begin{proof}
    Using the Lagrange form of the remainder (Theorem \ref{thm:ttl}), we have, for $x$ such that $|x-a|<\min\{\rho,R\}$,
    \[R_{n,a}(x)=\frac{f^{(n+1)}(\theta)}{(n+1)!}(x-a)^{n+1},\]
    for some $\theta$ between $a$ and $x$. Since $|x-a|<\min\{\rho,R\}$, and $\theta$ is between $a$ and $x$, we also have $|\theta-a|<\min\{\rho,R\}$, so for $n\ge N$, the derivative bound applies at $\theta$ also:
    \[
        |R_{n,a}(x)| = \left| \frac{f^{(n+1)}(\theta)}{(n+1)!} (x-a)^{n+1} \right| \le \frac{C \frac{(n+1)!}{R^{n+1}}}{(n+1)!} |x-a|^{n+1} = C \left( \frac{|x-a|}{R} \right)^{n+1}.
    \]
    Since, in particular, $|x-a|<R$ we know $\frac{|x-a|}{R} < 1$. Therefore, as $n \to \infty$, the remainder $|R_{n,a}(x)| \to 0$. This proves that the sequence of Taylor polynomials converges pointwise to $f(x)$.
\end{proof}

\begin{remark}
    The expression $\frac{(n+1)!}{R^{n+1}}$ is highly advantageous when bounding error because factorials grow significantly faster than exponentials. This affords us a massive amount of ``room'' to bound the derivatives of functions like $\sin(x)$, $\cos(x)$, and $\exp(x)$, which are indeed analytic functions.
\end{remark}

The distinction between infinitely differentiable and analytic is better understood via Complex Analysis (the analysis of $\C$). 
    
In $\R$, being infinitely differentiable is a surprisingly weak geometric condition. Because you can only approach a point from two directions (left or right) on the 1D real line, it is easy to ``cheat'' and smoothly glue a completely flat line to a curved function (like our bump function) without breaking differentiability. 
    
However, in the complex plane, a function takes inputs from a 2D surface. To be complex-differentiable at a point, the limit of the difference quotient must perfectly agree no matter which of the infinite possible directions you approach from. This geometric constraint is so incredibly rigid that if a complex function is differentiable just \textit{once} on an open set, it is automatically infinitely differentiable and perfectly equal to its Taylor series everywhere in that domain. In the complex world, the ``bump function'' is impossible.

\begin{exercise*}
    Prove that $\exp(x)$ is real analytic.
\end{exercise*}


\end{document}